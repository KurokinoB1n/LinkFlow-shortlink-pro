brokerClusterName=DefaultCluster
brokerName=broker-a
brokerId=0
deleteWhen=04
fileReservedTime=48
brokerRole=ASYNC_MASTER
flushDiskType=ASYNC_FLUSH
autoCreateTopicEnable=true
# brokerIP1 必须是客户端能访问到的 broker 地址：
# - 本地测试（应用在宿主机、broker 在 Docker）：127.0.0.1，走 10911 端口映射
# - 云上部署：改成云服务器可被客户端访问的 IP（局域网/公网）
brokerIP1=127.0.0.1
